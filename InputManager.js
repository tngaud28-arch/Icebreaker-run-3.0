export class InputManager{constructor(canvas,onMove,onPause){let start=null;addEventListener('keydown',e=>{let k=e.key.toLowerCase();if(['arrowleft','arrowright','a','d','escape'].includes(k))e.preventDefault();if(k==='arrowleft'||k==='a')onMove(-1);if(k==='arrowright'||k==='d')onMove(1);if(k==='escape')onPause()});canvas.addEventListener('pointerdown',e=>start=e.clientX);canvas.addEventListener('pointerup',e=>{if(start===null)return;let d=e.clientX-start;if(Math.abs(d)>35)onMove(d>0?1:-1);start=null})}}